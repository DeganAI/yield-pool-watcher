{
  "name": "yield-pool-watcher",
  "version": "1.0.0",
  "description": "Track APY and TVL across DeFi pools via x402",
  "type": "module",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "start": "bun run src/index.ts"
  },
  "dependencies": {
    "@lucid-agents/agent-kit": "^1.1.1",
    "hono": "^4.10.4",
    "zod": "^3.0.0"
  },
  "devDependencies": {
    "bun-types": "^1.0.0"
  }
}
